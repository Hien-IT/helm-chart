apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-service-node-port
  labels:
    app: minio
spec:
  type: NodePort
  ports:
    - port: 9000
      targetPort: 9000
      nodePort: 32900
    - port: 9001
      targetPort: 9001
      nodePort: 32901
  selector:
    app: minio