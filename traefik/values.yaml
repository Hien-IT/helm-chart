# Cấu hình ports
ports:
  web:
    port: 80
    exposedPort: 80
    protocol: TCP
  websecure:
    port: 443
    exposedPort: 443
    protocol: TCP

# Cấu hình service
service:
  type: LoadBalancer
  annotations: {}

# Bật dashboard
dashboard:
  enabled: true

# Cấu hình SSL
ssl:
  enabled: true
  permanentRedirect: false

