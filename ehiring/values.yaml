# Default values for ehiring.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

annotations: {}

global:
  ssl:
    enabled: true
  hosts: 
    - "10.86.62.31.sslip.io"
  frontend:
    env:
      - name: APP_API
        value: /api
      - name: AUTH_SECURE_ROUTES
        value: "https://10.86.62.31.sslip.io:30443/"
      - name: AUTH_ISSUER
        value: https://id.paas.ttgt.vn/realms/smc-staging
      - name: AUTH_CLIENT_ID
        value: ehiring
  backendService:
    env:
      - name: APP_NAME
        value: ehiring
      - name: APP_LOGGER_LEVEL
        value: debug,log,error,warn,verbose
      # - name: MONGODB_USERNAME
      #   value: ehiring
      # - name: MONGODB_PASSWORD
      #   value: ehiring
      - name: APP_TENANT
        value: stg
      - name: MONGODB_DATABASE
        value: ehiring-stg
      - name: MONGODB_HOSTNAME
        value: "mongodb-service"
      - name: MONGODB_PORT
        value: "27017"
      - name: APP_FRONTEND_URL
        value: https://10.86.62.31.sslip.io:30443/
      - name: APP_PREFIX
        value: api
      - name: APP_STANDALONE
        value: "false"
      - name: NATS_SERVERS_1
        value: nats://nats-service:4222
      - name: OIDC_CLIENT_ID
        value: ehiring
      - name: OIDC_ISSUER
        value: https://id.paas.ttgt.vn/realms/smc-staging
      - name: OIDC_JWKS_URI
        value: https://id.paas.ttgt.vn/realms/smc-staging/protocol/openid-connect/certs

imagePullSecrets:
  - name: rc-registry-secret
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

podAnnotations: {}

podSecurityContext:
  {}
  # fsGroup: 2000

securityContext:
  {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

service:
  type: ClusterIP
  port: 80

resources:
  {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

nodeSelector: {}

tolerations: []

affinity: {}

cms-ehiring-portal:
  service:
    ingress:
      type: frontend
  image:
    namespace: other
    repository: cms-ehiring-portal
  hosts: 
    - "tuyen-dung.tanlong.10.86.62.31.sslip.io"
    - "tuyen-dung.shlv.10.86.62.31.sslip.io"
  port: 3000
  router:
    routes:
      - path: /

ehiring-host:
  hosts: 
    - "tanlong.10.86.62.31.sslip.io"
    - "shlv.10.86.62.31.sslip.io"
  service:
    ingress:
      type: frontend
  router:
    routes:
      - path: /

competencies-backend:
  service:
    ingress:
      type: backend
  router:
    routes:
      - path: /api/competencies
      - path: /api/competency-groups

dashboard-backend:
  service:
    ingress:
      type: backend
  router:
    routes:
      - path: /api/dashboard

emails-backend:
  service:
    ingress:
      type: backend
  router:
    routes:
      - path: /api/emails

file-manager-backend:
  service:
    ingress:
      type: backend
  router:
    routes:
      - path: /api/file-manager

gpt-backend:
  service:
    ingress:
      type: backend
  router:
    routes:
      - path: /api/gpt

integrations-backend:
  service:
    ingress:
      type: backend
  router:
    routes:
      - path: /api/integrations

job-descriptions-backend:
  service:
    ingress:
      type: backend
  router:
    routes:
      - path: /api/job-descriptions
      - path: /api/job-description-groups

mail-templates-backend:
  service:
    ingress:
      type: backend
  router:
    routes:
      - path: /api/mail-templates

masterdata-backend:
  service:
    ingress:
      type: backend
  router:
    routes:
      - path: /api/masterdata

recruitment-backend:
  service:
    ingress:
      type: backend
  router:
    routes:
      - path: /api/recruitment-plans
      - path: /api/recruitment-requests
      - path: /api/applications
      - path: /api/offers

schedules-backend:
  service:
    ingress:
      type: backend
  router:
    routes:
      - path: /api/schedules
      - path: /api/schedule-groups
      - path: /api/evaluation-forms
      - path: /api/evaluation-form-groups

talent-pools-backend:
  service:
    ingress:
      type: backend
  router:
    routes:
      - path: /api/talent-pools
      - path: /api/talents
      - path: /api/resumes

users-backend:
  service:
    ingress:
      type: backend
  router:
    routes:
      - path: /api/users
      - path: /api/user-groups

tenants-backend:
  service:
    ingress:
      type: backend
  router:
    routes:
      - path: /api/tenants

websites-backend:
  service:
    ingress:
      type: backend
  router:
    routes:
      - path: /api/websites

portal-backend:
  hosts: 
    - "tuyen-dung.tanlong.10.86.62.31.sslip.io"
    - "tuyen-dung.shlv.10.86.62.31.sslip.io"
  service:
    ingress:
      type: backend
  router:
    routes:
      - path: /api/portals

services-mailer-tanlong:
  env:
    - name: MAILER_SENDER
      value: "smtp"
    - name: MAILER_HOST
      value: sandbox.smtp.mailtrap.io
    - name: MAILER_PORT
      value: "587"
    - name: MAILER_USERNAME
      value: 52a4acdc0ac586
    - name: MAILER_PASSWORD
      value: e2df8b6f5dfdc2
    - name: MAILER_FROM
      value: '"FPT Recruitment" <fpt@talentx.vn>'
    - name: MESSAGE_PATTERN_SEND_EMAIL
      value: "sendEmail.tanlong"
  image:
    repository: services-mailer
  router:
    enabled: false

services-mailer-shlv:
  env:
    - name: MAILER_SENDER
      value: "smtp"
    - name: MAILER_HOST
      value: sandbox.smtp.mailtrap.io
    - name: MAILER_PORT
      value: "587"
    - name: MAILER_USERNAME
      value: 5f9163fdea29ae
    - name: MAILER_PASSWORD
      value: 330fe63d79b821
    - name: MAILER_FROM
      value: '"FPT Recruitment" <fpt@talentx.vn>'
    - name: MESSAGE_PATTERN_SEND_EMAIL
      value: "sendEmail.shlv"  
  image:
    repository: services-mailer
  router:
    enabled: false    

services-cv-parser:
  router:
    enabled: false

services-integration-scheduler:
  router:
    enabled: false

services-email-scheduler:
  router:
    enabled: false

services-job-scheduler:
  router:
    enabled: false

services-gpt:
  router:
    enabled: false

services-export:
  router:
    enabled: false
